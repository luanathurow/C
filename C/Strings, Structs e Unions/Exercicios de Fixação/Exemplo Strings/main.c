#include <stdio.h>

#include <stdio.h>
#include <string.h>

int main (){

   /*char var[] = {'H', 'e', 'l', 'l', 'o', '\0'};

   for (int i = 0; i < 5; i++) {
      printf("%c", var[i]);
   }

   return 0;*/

   char hello[] = "Hello Luana";
   printf("%s\n", hello);

   return 0;
}
